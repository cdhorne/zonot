{
  "name": "zonot",
  "version": "0.0.0",
  "type": "module",
  "description": "Zonot CLI — calm capture, deep notes, plain Markdown in your own GitHub repo.",
  "license": "FSL-1.1-ALv2",
  "bin": {
    "zonot": "./dist/index.js"
  },
  "files": [
    "dist/index.js",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test --pass-with-no-tests",
    "dev": "bun run ./src/index.ts",
    "build": "bun run build:npm",
    "build:npm": "bun build ./src/index.ts --target node --external bun:sqlite --outfile ./dist/index.js",
    "build:bin": "bun build ./src/index.ts --compile --external node:sqlite --outfile ./dist/zonot",
    "build:dist": "bun scripts/build-binaries.ts",
    "prepublishOnly": "bun run build:npm"
  },
  "engines": {
    "node": ">=22",
    "bun": ">=1.2"
  },
  "devDependencies": {
    "@zonot/core": "workspace:*",
    "isomorphic-git": "^1.38.7"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  }
}
