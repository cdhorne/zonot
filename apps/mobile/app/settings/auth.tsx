import { Placeholder } from '@/components/Placeholder';

// Auth — Worker/workspace/status + sign-out (mobile-spec §9.2).
export default function AuthScreen() {
  return <Placeholder label="Auth (§9.2)" />;
}
