import { Placeholder } from '@/components/Placeholder';

// Sync details — the forensic queue/error screen (mobile-spec §4).
export default function SyncDetailsScreen() {
  return <Placeholder label="Sync details (§4)" />;
}
