import { Placeholder } from '@/components/Placeholder';

// Settings landing (Auth · Sync · About) — mobile-spec §4/§9.
export default function SettingsScreen() {
  return <Placeholder label="Settings" />;
}
