import { Placeholder } from '@/components/Placeholder';

// About — version · license · debug-bundle export (mobile-spec §8.1).
export default function AboutScreen() {
  return <Placeholder label="About" />;
}
