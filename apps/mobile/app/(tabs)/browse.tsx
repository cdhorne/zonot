import { Placeholder } from '@/components/Placeholder';

// Browse / search (mobile-spec §6) — lands in Phase 3(d).
export default function BrowseScreen() {
  return <Placeholder label="Browse (§6)" />;
}
