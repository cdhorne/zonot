import { Placeholder } from '@/components/Placeholder';

// Capture surface (mobile-spec §2) — lands in Phase 3(d).
export default function CaptureScreen() {
  return <Placeholder label="Capture (§2)" />;
}
