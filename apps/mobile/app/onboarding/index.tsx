import { Placeholder } from '@/components/Placeholder';

// First-launch onboarding (mobile-spec §9.1) — path-secret Worker URL + workspace.
export default function OnboardingScreen() {
  return <Placeholder label="Onboarding (§9)" />;
}
